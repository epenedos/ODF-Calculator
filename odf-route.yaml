kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: odf
  namespace: openshift-gitops
spec:
  to:
    kind: Service
    name: odf
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None
